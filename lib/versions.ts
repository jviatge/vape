import PackageJsonApp from "../../package.json";
import PackageJsonVape from "../package.json";

const versions = {
    app: PackageJsonApp.version,
    vape: PackageJsonVape.version,
};

export default versions;
